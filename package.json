{
  "name": "spacesmart-guide",
  "version": "1.0.0",
  "description": "SpaceSmart Guide - Smart Gear for Small Spaces",
  "private": true,
  "scripts": {
    "dev": "node scripts/build.js --dev",
    "build": "node scripts/build.js --prod",
    "validate": "node scripts/validate.js",
    "audit": "node scripts/audit-gsc.js",
    "clean": "rm -rf public",
    "prebuild": "npm run clean",
    "postbuild": "npm run validate"
  },
  "dependencies": {
    "nunjucks": "^3.2.4",
    "yaml": "^2.4.0",
    "glob": "^10.3.10",
    "cheerio": "^1.0.0-rc.12",
    "axios": "^1.6.7",
    "date-fns": "^3.3.1",
    "slugify": "^1.6.6"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}